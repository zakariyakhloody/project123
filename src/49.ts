export function yourFunction(param1: any, param2: string): Promise<any> {
    // Code to be executed when the function is called
}

// Example usage
const result = yourFunction(10, "Hello, World!");
console.log(result);
